<% content_for :title, "Edit Agent - #{@agent.name}" %>

<div class="min-h-screen bg-gray-50">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="mb-8">
      <%= link_to agent_path(@agent), class: "text-sm text-gray-500 hover:text-gray-700 mb-2 inline-block" do %>
        ← Back to Agent
      <% end %>
      <h1 class="text-3xl font-bold text-gray-900">Edit Agent</h1>
      <p class="mt-2 text-sm text-gray-700">Update agent configuration</p>
    </div>

    <div class="bg-white shadow sm:rounded-lg">
      <div class="px-4 py-5 sm:p-6">
        <%= form_with(model: @agent) do |form| %>
          <%= render "form", form: form %>

          <div class="mt-6 flex items-center justify-end gap-x-3">
            <%= link_to "Cancel", agent_path(@agent), class: "text-sm font-semibold leading-6 text-gray-900" %>
            <%= form.submit "Update Agent",
                class: "inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
